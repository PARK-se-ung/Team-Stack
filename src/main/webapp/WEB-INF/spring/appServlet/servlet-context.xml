<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:websocket= "http://www.springframework.org/schema/websocket"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
                           http://www.springframework.org/schema/websocket  http://www.springframework.org/schema/websocket/spring-websocket.xsd">

    <!--어노테이션 bean 불러올 패키지 설정 -->
    <context:component-scan base-package="org.ts.teamstack"/>

    <!-- spring-web mvc모듈 클래스 설정하기 -->
    <mvc:annotation-driven/>

    <!-- 정적요소 경로 등록하기 -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>

    <!-- 기본적인 viewResolver구현체 등록하기 -->
    <!--jsp화면을 처리하는 resolver-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--  파일업로드 + 일반 폼 필드  -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="10485760"/> <!-- 10MB -->
    </bean>

    <!--objectMapper 추가-->
    <bean id="objectMapper" class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <!--웹소켓 핸들러-->
    <bean id="chatting" class="org.ts.teamstack.classpage.websocket.ChattingServer"/>
    <websocket:handlers>
        <websocket:mapping path="/chat" handler="chatting"/>
        <websocket:handshake-interceptors>
            <bean class="org.springframework.web.socket.server.support.HttpSessionHandshakeInterceptor"/>
        </websocket:handshake-interceptors>
    </websocket:handlers>

</beans>